# ブロック崩しTODO

設計上の決定は`SPEC.md`へ、未完了の実装・試験・未決定事項は本ファイルへ記録する。

## 実機確認済み

- [x] OLEDを3.3V、SDA=PC1、SCL=PC2、アドレス`0x3C`で表示する。
- [x] ロータリーをS1=PC5、S2=PD0、KEY=PC3で読み取る。
- [x] ロータリーでOLED上の文字を左右へ動かす。
- [x] KEY押下で画面を消去して文字を再表示する。
- [x] 振動モジュールをVCC=5V、GND=共通、IN=PC4で制御する。
- [x] 3部品を同時接続して動作させる。
- [x] 振動レベル25では弱いことを確認し、試験用フィードバックをレベル50へ変更する。
- [x] MakefileからDevkitと同梱ツールを自動検出し、起動コマンドなしでビルド可能にする。
- [x] 起動コマンドなしのクリーン環境で`make`と`make size`が成功することを確認する。
- [x] 起動コマンドなしで`make flash`が正しい書き込みコマンドを生成することを確認する。
- [x] このフォルダを独立したGitリポジトリとして`main`ブランチで初期化する。
- [x] 必要な9ファイルをGitの初回コミット候補としてステージする。
- [x] Gitのメールアドレスをリポジトリへ設定し、初回コミットを作成してGitHubへpushする。
- [x] `workspace/exercises`への固定配置をMakefileから取り除く。
- [x] Devkit内では上位フォルダを探索してDevkitルートを自動検出する。
- [x] Devkit外ではGit管理外の`local.mk`でDevkitルートを指定できるようにする。
- [x] 対応環境をApple Silicon搭載Macに限定し、WindowsとIntel Macを対象外と明記する。

## ゲーム実装

- [ ] 現在の統合試験コードを`block_breaker.c`へ整理する。
- [ ] 1024バイトの1bitフレームバッファと描画関数を追加する。
- [ ] 8列×3行のブロックを描画する。
- [ ] 幅11、高さ2のバーをy=57へ描画する。
- [ ] 1×1のボールをバー中央上へ描画する。
- [ ] `READY`、`PLAYING`、`GAME_OVER`、`GAME_CLEAR`の状態管理を追加する。
- [ ] READY中にロータリーでバーとボールを一緒に動かす。
- [ ] KEY押下でバー位置に応じた左右45度方向へボールを発射する。
- [ ] 50ms周期のボール移動を追加する。
- [ ] 左右壁、上壁、バーとの衝突と反射を追加する。
- [ ] ブロックとの衝突、反射、1個消去を追加する。
- [ ] ブロック破壊時にレベル50、30ms振動する。
- [ ] ボールが下端へ到達したらゲームオーバーにする。
- [ ] `GAME OVER`を中央表示し、レベル80で2000ms振動する。
- [ ] GAME OVER中のKEY押下で振動を止め、READYへ戻す。
- [ ] 全ブロック破壊時に`GAME CLEAR`を表示する。
- [ ] OLEDの変更ページだけを更新する処理を追加する。
- [ ] OLEDエラー時に振動を停止してLEDで通知する。

## 未決定事項

- [ ] `GAME CLEAR`時の振動パターンを決める。
- [ ] ボール速度50msが遊びやすいか実機で調整する。
- [ ] ロータリー1クリック2ピクセルが操作しやすいか実機で調整する。
- [ ] バー反射の左右判定を3分割より細かくするか検討する。
- [ ] 得点表示を初版へ含めるか決める。
- [ ] 難易度上昇としてボール加速を入れるか決める。

## 完成確認

- [ ] 起動時に上部約30%へブロック、中央下へバーとボールが表示される。
- [ ] 押しっぱなしではボールが複数回発射されない。
- [ ] バーが画面外へ出ない。
- [ ] ボールがブロックを飛び越えない。
- [ ] ブロック破壊時の振動が短く、自動停止する。
- [ ] ゲームオーバー振動が約2秒で自動停止する。
- [ ] 再スタートで全状態が初期値へ戻る。
- [ ] 10分以上プレイして表示乱れ、連続振動、リセットがない。
