# ブロック崩し仕様

## 目的

UIAPduino Pro Micro CH32V003 V1.4、SSD1306 OLED、押し込みスイッチ付き
ロータリーエンコーダー、振動モジュールを使い、PCなしで遊べるブロック崩しを作る。

設計上の決定は本ファイルへ、未完了の実装・試験・未決定事項は`TODO.md`へ記録する。

## 確認済みハードウェア

| 部品 | 部品側端子 | UIAPduino | ch32fun | 条件 |
|---|---|---|---|---|
| OLED | SCL / SCK | D4 | PC2 | ソフトウェアI2C |
| OLED | SDA | D3 | PC1 | ソフトウェアI2C |
| OLED | VDD | 3.3V | - | 正しい3.3V端子を使う |
| OLED | GND | GND | - | 共通GND |
| ロータリー | S1 | D7 | PC5 | 内部プルアップ入力 |
| ロータリー | S2 | D10 | PD0 | 内部プルアップ入力 |
| ロータリー | KEY | D5 | PC3 | 押下時Low |
| ロータリー | 5V | 5V | - | モジュール電源 |
| ロータリー | GND | GND | - | 共通GND |
| 振動 | IN | D6 / A2 | PC4 / TIM1_CH4 | 500Hz PWM |
| 振動 | VCC | 5V | - | モジュール電源 |
| 振動 | GND | GND | - | 共通GND |

OLEDは128×64、I2Cアドレス`0x3C`、SSD1306互換として動作を確認済みである。
3部品を同時接続し、OLED表示、ロータリー回転と押下、PC4の振動を実機確認済みである。
OLEDのVDDを誤った3.3V位置へ挿すとACKせず表示できないため、配線位置を固定する。

## 画面レイアウト

座標原点は左上、x=0〜127、y=0〜63とする。

| 要素 | 初期仕様 |
|---|---|
| ブロック | 16列×3行、上端約30%のy=1〜19 |
| ブロック寸法 | 7×5ピクセル、横間隔1、縦間隔2 |
| バー | 22×2ピクセル |
| バー位置 | y=57〜58。画面下に5ピクセル空ける |
| バー初期x | 53。画面中央付近 |
| ボール | 3×3ピクセル |
| 待機中のボール | バー中央の3ピクセル上、y=54〜56 |

## ゲーム状態

### `READY`

- 起動時と再スタート時の状態。
- ブロックを初期配置し、バーを中央へ戻す。
- ボールはバー中央上に載せ、ロータリーに合わせてバーと一緒に左右移動する。
- KEYの新しい押下でボールを発射して`PLAYING`へ移る。

### `PLAYING`

- ロータリー1クリックでバーを4ピクセル移動し、画面外へ出さない。
- ボールは初期値として35msごとにx、yを各1ピクセル進める。
- 発射角は水平基準で約60度とし、縦5ピクセルに対して横3ピクセル進める。
- 縦速度は上向き`-1`とし、横移動は整数の累積値で間引く。
- バー中心が画面中央より左なら右上、中央以上なら左上へ発射する。
- 左右と上の壁、バー、ブロックで反射する。
- 1回の更新で1ピクセルだけ進め、ブロックを飛び越えないようにする。
- ブロックへ当たったらそのブロックを1個消し、短い振動を行う。
- ボールがバーより下へ抜け、画面下端へ到達したら`GAME_OVER`へ移る。

### `GAME_OVER`

- 画面中央へ`GAME OVER`を表示する。
- 振動レベル80、500Hz PWMで約1500ms振動し、必ず自動停止する。
- KEYの新しい押下で振動を停止し、ブロック、バー、ボールを初期化して`READY`へ戻る。

### `GAME_CLEAR`

- 全ブロックを消したらボールを停止し、画面中央へ`GAME CLEAR`を表示する。
- 画面の2か所で花火が広がる4段階のアニメーションを繰り返す。
- 振動方法は実装前に`TODO.md`の未決定事項を確定する。
- KEYの新しい押下で`READY`へ戻る。

## 衝突と操作

- ボールの次の座標を求めてから、壁、バー、ブロックの順に衝突を判定する。
- バーについては現在位置だけでなく、前回のボール更新後に通過した横範囲全体を
  衝突対象とし、4ピクセル移動時のすり抜けを防ぐ。
- バーの左側に当たれば左上、右側に当たれば右上へ反射する。
- バー中央に当たった場合は現在の横方向を維持して上向きへ反射する。
- 最後のロータリー操作から120ms以内にバーへ当たった場合は、バーの進行方向へ
  ボールを反射し、横移動を縦5ピクセルに対して4ピクセルへ増やす。
- 同じ更新で複数ブロックを消さず、最初に衝突した1個だけを消す。
- KEYは20msデバウンスし、押しっぱなしを複数回の押下として扱わない。

## 振動

- PC4は起動直後にGPIO Lowへ固定し、意図しない振動を防ぐ。
- レベル1〜99はTIM1_CH4の500Hz PWM、停止時はGPIO Lowとする。
- ブロック破壊時の初期値はレベル50、30msを1回とする。
- ゲームオーバーはレベル80、1500msを1回とする。
- 有限時間後に必ず自動停止し、ゲーム処理や表示をブロックしない。

## 表示と時間管理

- 1bit、1024バイトのフレームバッファを使う。
- OLEDは動作確認済みの初期化順序、1文字単位の転送、バス復旧処理を維持する。
- 毎ループ全画面転送はせず、変更したページ内でも変更された横範囲だけを更新する。
- ロータリー読取りがOLED転送で長時間止まらないよう、通信速度より転送量の削減を優先する。
- ボール更新、入力読取り、振動停止を独立した経過時間で管理する。
- 浮動小数点は使わず、座標と速度は整数で保持する。
- OLED通信失敗時はゲームを開始せず、振動を停止して基板LEDを点滅する。

## 安全条件

- 配線変更前にUSBケーブルを外す。
- 全モジュールでGNDを共有する。
- モーター本体をGPIOへ直結せず、振動モジュールのINだけをPC4へ接続する。
- 発熱、異臭、停止しない振動、反復リセット、USB切断があれば直ちにUSBを外す。
- 振動レベルを上げる場合も定格電圧を超えず、必ず自動停止時間を設定する。

## ビルド

本プロジェクトは、XP祭り2026ワークショップ向けの
[`amapyon/xpfes2026`](https://github.com/amapyon/xpfes2026)で配布・案内される
UIAP Devkitを開発基盤とする。ビルドと書き込みにはDevkit内の`ch32fun`、
RISC-Vツールチェーン、`minichlink`を必要とし、本リポジトリへは複製しない。
本プロジェクトはDevkitを利用した独立作品であり、`xpfes2026`の公式演習・公式サンプル
としては扱わない。

対応環境はApple Silicon搭載Macとする。WindowsおよびIntel Macは未検証ではなく、
現行のmacOS arm64用Devkitランタイムを使用できないためサポート対象外とする。

Makefileは現在位置から上位フォルダを探索し、UIAP Devkitを自動検出する。
`workspace/exercises`という固定配置は要求しない。リポジトリがDevkit外にある場合は、
Git管理外の`local.mk`で`UIAP_DEVKIT_ROOT`を指定する。

```text
make
make size
make flash
```

`make flash`の前にUIAPduinoを書き込み待機状態へする。
